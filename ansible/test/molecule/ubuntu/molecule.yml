---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: ubuntu
    image: geerlingguy/docker-ubuntu2004-ansible
provisioner:
  name: ansible
  lint: ansible-lint
  config_options:
    defaults:
      callback_whitelist: profile_tasks
verifier:
  name: ansible
  lint: yamllint
